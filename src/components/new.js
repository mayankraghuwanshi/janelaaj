import React, {Component} from 'react';
import {Input, LabelInputOption} from "../helpers/Helpers";

class New extends Component {
    render() {
        return (
            <div>
              <Input/><Input/>
            </div>
        );
    }
}

export default New;